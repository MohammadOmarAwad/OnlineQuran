name: DeployTo_gh_pages

on:
  push:
    branches: [ "main" ]

jobs:
  build:

    # using Ubuntu
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1 
      with:
        node-version: '10.x'

    - uses: actions/cache@v1 
